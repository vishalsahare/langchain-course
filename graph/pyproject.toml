[project]
name = "graph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vishal Sahare", email = "vsahare@cisco.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
graph = "graph:main"

[build-system]
requires = ["uv_build>=0.11.31,<0.12.0"]
build-backend = "uv_build"
